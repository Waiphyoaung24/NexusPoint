import { describe, it } from "vitest";

describe("FloorPlanEditor", () => {
  it("renders canvas with tables", () => {
    // Test: tables rendered at correct positions
  });

  it("shows config panel when table is selected", () => {
    // Test: clicking a table shows seat/shape config
  });

  it("shows unsaved changes badge after dragging", () => {
    // Test: dirty state tracked
  });
});
